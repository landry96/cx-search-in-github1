{
    "main": "src/server.js",
    "scripts": {
        "dev": "yarn nodemon",
        "migration:run": "yarn sequelize db:migrate --config sequelize.config.js",
        "migration:undo": "yarn sequelize db:migrate:undo --config sequelize.config.js"
    },
    "devDependencies": {
        "nodemon": "^2.0.7",
        "sequelize-cli": "^6.2.0"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^8.2.0",
        "express": "^4.17.1",
        "node-fetch": "^2.6.1",
        "pg": "^8.5.1",
        "sequelize": "^6.3.5"
    }
}
